"""
特徵因子模組（features）— 對應 ADR-101
========================================
每個影響因子一個獨立模組，各自負責：載入原始資料 → 對齊到 (站點/縣市, 時間) → 輸出特徵欄位。
初期只做單因子，不處理交叉影響（交叉留待後續 ADR）。

四個因子（ADR-101）：
  - calendar_holiday：假日因子（政府辦公日曆）
  - school_calendar：學期/學生數因子（寒暑假 + 教育部各級學校縣市學生數）
  - weather：天氣因子（CODiS 重建歷史逐時；訓練用歷史，現況預測才接即時 API）
  - terrain：地形因子（座標→坡度→分類）

共同設計：
  - 每個因子模組對外暴露 build_feature(...) 或等價函式，回傳可併入訓練表的欄位
  - 資料載入失敗要明確（NFR-5），不靜默給預設值
  - 參數（門檻/路徑）走 config.yaml，不寫死
"""
