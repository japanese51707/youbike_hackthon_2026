# 進訓練前審查報告 v1 — 模型架構與特徵設計

> 審查日期：2026-09-03　審查者：外部 AI（Claude）
> 審查對象：ADR-001／002／004／011～014 所定之模型架構與四批特徵因子設計
> 審查範圍：**進入模型訓練前**的設計缺陷、資料陷阱、被高估／低估的部分
> 前一份架構審查：`review_architecture_v2.md`（本報告不取代它，關注面不同）

## 給接手修正的 AI agent 的使用說明

1. **先讀 `AGENTS.md`、`.kiro/steering/decision_governance.md`、`docs/decisions/README.md`**，本報告不取代它們。
2. **核准閘門仍然有效**：動任何檔案前先說明計畫（目標、影響檔案、架構影響、風險、驗證指令），等使用者核准。
3. **ADR 治理**：本報告第 2 節已預先做了 ADR 影響判斷，但那是**建議**，不是核准。需要新增 ADR 的項目一律先建 `proposed`，等 owner 核准才實作。
4. **不要一次做完**。11 條發現照 P0 → P1 → P2 分批，**P0 三條做完必須先重跑一次驗證、拿到新數字**，因為 P0 會改變所有既有結論。
5. **核心約束不可違反**（ADR-004）：AI 只做估計，確定性規則引擎才能做調度決策；規則引擎吃區間下界，不吃點估計。本報告沒有任何一條挑戰這個約束。
6. **不要動的東西**：`youbike資料集/`、`output/youbike_parquet/`、`.venv/`、`config.poc_v1.yaml`、`rule_engine.poc_v1.py`。

---

## 1. 總評

架構方向正確，**ADR-001／002／004 三個核心決策不需要更動**。分層參數、quantile 出區間、AI 估計與規則決策分離——這三個是對的，可以安心往下走。

問題集中在**特徵計算方式與驗證協定**，不在架構選型。分三類：

| 類別 | 數量 | 嚴重度 |
|------|------|--------|
| 會讓現有數字失效（必須先修） | 3 | **P0** |
| 會讓模型在關鍵區域失準 | 4 | P1 |
| 效率與說服力（砍冗餘、補指標） | 4 | P2 |

**最重要的一句話**：`MAE 1.51 / baseline 4.29 / 區間覆蓋 78.7%` 這組數字在 P0 修完之前**不可對外宣稱**。ADR-002 的「理由」段目前引用了這組數字，需在重測後更正。

---

## 2. ADR 影響判斷總表（Kiro 動手前先看這張）

| ID | 發現 | 涉及 ADR | ADR 判斷 |
|----|------|----------|----------|
| F-01 | 站點行為指紋用全期資料計算 → 資料洩漏 | ADR-104 | **不需新 ADR**。屬實作缺陷，非決策改變。但需在 ADR-104 補述「特徵計算窗口」約束（澄清性補記，標 `retrospective: true`） |
| F-02 | baseline 過弱，改善幅度可能虛胖 | ADR-002 | **不需新 ADR**。但 ADR-002「理由」段的實測數字須於重測後更正 |
| F-03 | 目標變數在空／滿站被截斷（censoring） | — | **需新增 proposed ADR**：目標變數定義與截斷樣本處理 |
| F-04 | 缺多視野（multi-horizon）預測 | ADR-002 / ADR-004 / `api_contract.md` | **需新增 proposed ADR**：改變推論方式與 API Schema（跨人契約） |
| F-05 | 分位數不可加（多步區間算錯） | 併入 F-04 | 同上 |
| F-06 | 站點識別特徵不足，缺同時段歷史 profile | ADR-001 | **沿用 ADR-001**。屬其「站點條件參數」之具體化，補特徵即可；建議於 ADR-103 補記 |
| F-07 | 驗證協定：單一時間切分不足 | — | **需新增 proposed ADR**：驗證協定（決定什麼數字可對外宣稱，屬跨人契約） |
| F-08 | 因子冗餘，5～6 個模組邊際價值趨近零 | ADR-102 / ADR-103 | **先做消融實驗，用資料決定，再開 ADR supersede**。不得在無證據下靜默砍除 accepted 決策的內容 |
| F-09 | 缺決策層驗收指標 | ADR-004 | **需新增 proposed ADR**：三層驗收指標（模型／決策／營運） |
| F-10 | 不確定性只做了 aleatoric，缺分布外偵測 | ADR-006 | **沿用 ADR-006**（可抽換資料源與明確降級），屬其降級觸發條件之擴充 |
| F-11 | 服務公平性／區域均衡未納入 | — | 建議新增 proposed ADR（政策對齊，非技術必要） |

---

## 3. P0：三條必須先修，否則所有數字待定

### F-01｜站點行為指紋幾乎確定造成資料洩漏 🔴

**現況**：ADR-104 定義的行為指紋（日夜活動比、平假日比、早晚峰方向、峰型、需求密度、空滿頻率）寫的是「六個月歷史算」。ADR-103 的歷史空滿頻率、波動度、日均周轉量同理。

**問題**：若用 2026-01～06 全部資料計算，再拿去餵「1～5 月訓練、6 月驗證」，**6 月的資訊已經進入特徵**。這會讓驗證分數虛高，且不會報錯——只會在上線後才發現落差。

波及範圍：ADR-103 的「站點時序自身」批 + ADR-104 的「行為指紋」批，**約佔全部特徵模組的一半**。

**修法**：
- 所有歷史統計量類特徵，只能用**訓練期**資料計算；或改用 expanding window（每個時點只用該時點之前的資料）。
- 站型分群的 fit 同樣只能用訓練期。
- 上線後採滾動計算，讓訓練與推論邏輯一致。
- 建議把「特徵計算窗口」寫成 pipeline 層級的強制約束，而非各特徵自行處理。

**驗證**：修正前後的驗證 MAE 必須同時記錄。若修正後 MAE 明顯變差，代表洩漏確實存在且量級不小——那是好消息，表示抓對了。

### F-02｜baseline 過弱，64.7% 的改善幅度可能虛胖 🔴

**現況**：ADR-002 記載 `MAE 1.51，baseline 4.29，改善 64.7%`。

**問題**：baseline 定義不明。若是「預測 Δ=0」或全域平均，這個對比沒有說服力。

**正確 baseline**：**seasonal naive** — 「該站 × 該 day_type × 該時段的歷史中位數」。零成本查表，在這類問題上通常強得驚人。

**為什麼非改不可**：這是評審最可能問的一刀——「你的模型比單純查歷史平均好多少？」。答不出來，整個 ML 的必要性就被質疑。

**兩種結果都是好消息**：
- 贏很多 → 模型價值被證明，而且是用嚴格的對照證明的
- 贏不多 → 你及早發現真正該投資的是規則層與調度優化，而非模型精度。這個轉向本身就是成熟判斷

**修法**：實作 seasonal naive baseline，與 LightGBM 在**同一份驗證集**上比較。同時保留舊 baseline 作為對照（不刪，標明各自定義）。

### F-03｜目標變數在空／滿站被截斷，而那正是系統存在的理由 🔴

**問題**：站空之後仍有人想借車，但**觀測到的 Δ 是 0**——因為沒車可借。滿站對稱。

這代表訓練資料在觸底／觸頂區域是**被截斷的（censored）**，真實需求被物理邊界切掉。模型學到「空站之後不會再變少」，於是**系統性低估空站的嚴重程度**。

而空站正是整個系統存在的理由。**這是本專案最大的統計陷阱，且正好落在最關鍵的區域。**

**修法（依成本排序，至少做 1、2）**：
1. 明確標記觸底／觸頂樣本（可借=0 或 可還=0），訓練時排除或降權——它們的目標值是假的
2. 評估時**分區間**：健康水位 / 接近空 / 已空，三段分開報 MAE 與區間覆蓋率
3. 進階：用同群站在同時段**未觸底時**的流量，插補被壓抑的真實需求

第 3 項若做出來，是簡報上很有份量的一段：「我們發現官方快照資料低估了空站的真實需求缺口，並做了還原」。

---

## 4. P1：會讓模型在關鍵區域失準

### F-04｜缺多視野（multi-horizon）預測，與規則引擎的需求對不上 🟠

**現況**：模型預測「下一時段（30 分鐘）」的淨流入／流出。

**問題**：ADR 系列與 `design.md` 的核心設計是「緊急度看**調度員到達時**的存量」，而到達時間是變動的（30～90 分鐘不等）。單一 t+1 的模型給不出這個。

目前 `rule_engine.py` 用「調整後到達存量 = 當前存量 −（當前存量 − 預測區間下界）× 觸發靈敏度」來近似——這是用一個 30 分鐘的預測去外推更長的到達時間，**靈敏度參數實際上在做視野外推的工作**，那不是它該負責的事。

**兩種修法**：
- ❌ 遞迴（把預測值當下一步輸入）→ 誤差累積，且分位數無法遞迴
- ✅ **直接多視野：訓練 h=1,2,3,4 四個獨立目標**（30 / 60 / 90 / 120 分鐘）

成本是 4 倍訓練時間（LightGBM 很快，可接受），換來穩定的多步預測與正確的區間。

**影響 `api_contract.md`**：預測 Schema 需增加 horizon 維度，屬跨人契約變更 → 需 proposed ADR，且要通知 B、C。

### F-05｜分位數不可加，多步區間會算錯 🟠

**問題**：若算了四個 30 分鐘 Δ 的 P10 然後相加，當作 2 小時存量的 P10——**數學上是錯的**。分位數不具可加性，相加會低估區間寬度（實際覆蓋率會遠低於名目值）。

**修法**：直接對「h 期**累積** Δ」訓練分位數模型。這是走 F-04 直接多視野的第二個理由。

### F-06｜站點識別特徵不足，缺最強的那一個 🟠

**先確認一件事**：**不要把 `station_id` 當類別特徵**。1,583 個類別會過擬合，且無法泛化到新站——那會直接摧毀 ADR-001 承諾的冷啟動能力。

**建議的站點畫像特徵**：

| 特徵 | 狀態 |
|------|------|
| 柱數（站點容量） | ADR-103 已有 |
| 行為指紋（訓練期算） | ADR-104 已有，但需修 F-01 |
| **該站 × 該 day_type × 該時段的歷史 P50 淨流量** | **缺，最重要** |
| 站型分群 ID（4～6 類，非 1583 類） | ADR-104 已有 |
| lat / lon | 建議補：樹模型能用它切出空間區塊，意外有效 |
| 鄰近站聚合狀態 | ADR-103 已有 |

**為什麼「同時段歷史 profile」是最重要的一個**：它把「每站一個模型」的資訊壓縮成一個數字，交給單一模型使用。通常是這類問題**最強的單一特徵**，而且——這才是重點——**它讓 ADR-001 的第①層規則化基礎與第②層 AI 最適化在特徵層真正接起來**，不再只是概念上的分層。

（前提：這個 profile 只能用訓練期計算，見 F-01。）

**另一個相關問題：目標尺度。** 60 柱大站與 12 柱小站，Δ=5 意義完全不同。建議實驗 `目標 = Δ / 柱數`，推論時乘回。兩個版本都跑一次比較（約 1 小時實驗）。同時也代表：**現有 MAE 1.51 是全站平均，會被大站主導，必須分站型報。**

### F-07｜驗證協定：單一時間切分有季節陷阱 🟠

**現況**：1～5 月訓練、6 月驗證，禁止隨機切分。**方向正確**（禁止隨機切分是必須的），但不夠。

**四個問題**：
1. 6 月是梅雨末／初夏，與 1～5 月（冬春）氣候不同，天氣係數未必適用
2. 1～2 月含春節，訓練集有一大塊異常期（需先標為 day_type，見 review_architecture_v2 相關項）
3. **只有一個驗證窗口 = 一個樣本**，無從得知 1.51 是穩定還是運氣
4. **年度事件盲區**：颱風、跨年、開學季、週年慶、地方選舉全都不在 1～6 月資料內

**修法：rolling-origin CV**

```
Fold 1: 訓練 1-3月 → 驗證 4月
Fold 2: 訓練 1-4月 → 驗證 5月
Fold 3: 訓練 1-5月 → 驗證 6月
```

報 **平均 ± 標準差**，不是單一數字。那個「±」在簡報上比「1.51」更有說服力——它顯示團隊在質疑自己的結果。

**兩個技術細節**：
- 做多視野（h=4）時，訓練／驗證邊界要留 **h 個時段的 gap**，避免目標跨越邊界。單期 t+1 則不需要。
- **CV 用來選超參數，不是用來丟資料**。選定後，最終上線模型用**全部六個月**重訓。

**第 4 點的結論很重要**：模型結構性地沒見過半年份的事件，**所以 ADR-001 的第③層規則覆寫不是可選項，是必需品**。這句話應主動寫進簡報——它同時解釋了三層架構的必要性。

---

## 5. 其他資料洩漏風險（除 lag 類外）

團隊已處理 lag／變化率／鄰近站的時序對齊。以下是**尚未處理**的六項：

| # | 風險 | 說明 | 嚴重度 |
|---|------|------|--------|
| 1 | **缺失值填補** | 30 分鐘快照必有缺漏。若用 `interpolate()` 前後插值，就用到了未來值。**只能 forward fill**。一行程式碼就中招 | 高 |
| 2 | **調度介入標註的離線／線上不對稱** | `analysis_rebalancing_detection_v2.py` 若用「之後是否回落」判斷，那是離線標註，**上線做不到**。必須明確分兩套：離線標註（可看未來，用於清訓練資料）vs 線上偵測（只能看過去）。混用會讓離線效果無法複製 | 高 |
| 3 | **天氣的 train-serving skew** | CODiS 是**實測值**。訓練用實測、上線用預報 → 離線好、上線差。不是傳統洩漏但後果一樣。**修法：訓練時對天氣真值注入雜訊模擬預報誤差**，或改用歷史預報存檔 | 中 |
| 4 | **停班停課標籤來源** | 前一晚公告（→ 已知未來，合法）還是事後看資料標的（→ 洩漏）？需確認 | 中 |
| 5 | **站型分群 fit 範圍** | 與 F-01 同一問題 | 高 |
| 6 | **CODiS 空間對應誤差** | 1,583 站對應有限測站，山區站點距離可能很遠。非洩漏，但是被低估的誤差來源 | 低 |

**另有一項不是洩漏但會誤導評估**：特徵含 `可借車數(t)`，目標是 `Δ(t→t+1)`。在邊界上，特徵**機械地決定**目標的取值範圍（可借=0 時 Δ 不可能為負）。模型會學到這個恆等式，於是在邊界區域「表現異常好」——那不是預測能力。**分區間評估才看得出來**（見 F-03 修法第 2 項）。

---

## 6. 區間與規則引擎介面（ADR-002 / ADR-004 相關）

quantile regression 出區間、規則引擎吃下界——**方向正確，可以安心**。以下是五個實作坑：

### 6.1 Quantile crossing（一定會遇到）
獨立訓練 P10／P50／P90 會出現 `P10 > P90`（稀疏區域尤其常見）。需加後處理排序，或用單調約束。

### 6.2 分位數要跟營運成本掛鉤，不能拍腦袋
「防空站吃下界」對，但下界是 P10 還是 P5？答案來自 newsvendor：

```
最適分位數 = 漏報成本 / (漏報成本 + 誤報成本)
```

漏報 = 該調度沒調度（民眾借不到車）；誤報 = 白跑一趟（人力油耗）。若漏報成本是誤報的 5 倍，最適分位數約 P17。

**這件事的價值不只是技術正確**——它把分位數從技術黑箱變成可以跟交通局討論的**營運參數**：「你們覺得白跑一趟 vs 民眾借不到車，哪個比較痛？」。建議把它外部化到 `config.yaml`，與現有的 `trigger.觸發靈敏度` 並列。

### 6.3 覆蓋率驗證：整體 PICP 不夠，必須分層
現有數字是整體 78.7%。但整體 78.7% 很可能是「健康區 88% + 危險區 55%」的平均——**而危險區正是實際使用的區域**（樣本少 + 截斷效應雙重打擊）。

分層維度：存量水位（健康／接近空／已空）、站型、時段、平假日。

### 6.4 只看覆蓋率會被騙
區間拉到無限寬，覆蓋率 100%，毫無用處。必須同時報：
- **PICP**（覆蓋率）
- **MPIW**（平均區間寬度）
- 或用 **pinball loss / Winkler score**，同時懲罰兩者

再畫一張**可靠度圖**（名目分位數 vs 實際覆蓋率，理想為對角線）——這是很好的簡報圖。

### 6.5 建議加 split conformal prediction 做事後校準
留一段校準集，算殘差分位數，直接調整區間寬度。實作簡單（數十行），但它給的是**有限樣本的覆蓋保證**。

「我們的 90% 區間，數學上保證覆蓋 90%」在政府案的份量遠大於「我們調參調到 90%」。**CP 值最高的加分項之一。**

---

## 7. P2：因子冗餘（F-08）

**直說：15 個因子模組裡，我判斷 5～6 個對預測精度的貢獻趨近於零。**

⚠️ **但這些都在 accepted 的 ADR-102／013 裡，不得無證據靜默砍除。** 正確流程是：先做消融實驗 → 拿到證據 → 開 proposed ADR supersede 相關條目 → owner 核准後才砍。

| 因子 | 判斷 | 理由 | 出處 |
|------|------|------|------|
| **日出日落（天文公式）** | 建議砍或併入 `hour × month` | 與「時段 × 月份」高度共線，樹模型從原始欄位就能學到 | ADR-102 |
| **地形坡度** | 建議降級為冷啟動／簡報用 | 靜態特徵，效應**已完全體現在該站的 lag 與行為指紋裡**。與 ADR-104 把學生數降級的理由完全相同 | ADR-101 |
| **POI 距離 9 類** | 建議壓縮至 2～3 個，或整批移出模型 | 行為指紋是 POI 的「果」，有果就不需要因。保留理由是 ADR-104 的「兩套分群對照」——那是**分析產物，不是模型特徵**。若要留，只留「距最近捷運出口距離」 | ADR-102 |
| **事件（活動路跑）** | 建議不進模型，走第③層規則覆寫 | 六個月樣本太少，學不出係數 | ADR-102 |
| **特殊天氣（颱風／豪大雨／停班課）** | 建議不進模型，走第③層規則覆寫 | **1～6 月台灣幾乎沒有颱風，訓練資料裡沒有樣本** | ADR-102 |
| **溫度倒 U 舒適度** | 建議改回原始溫度 | LightGBM 自己會切分箱，人工做倒 U 是多餘的 | ADR-103 |
| **加速度（二階差分）** | 待消融驗證，判斷為雜訊 | 30 分鐘粒度下二階差分訊噪比很差 | ADR-103 |
| **波動度／空滿頻率／日均周轉／需求密度** | 可能嚴重共線，建議保留 2 個 | 都在量「這站有多忙、多不穩」。先跑相關矩陣 | ADR-103／014 |

砍完後的核心特徵約 15～20 個欄位：
`lag 族 + 時間編碼 + day_type + 站點識別（含同時段 profile）+ 指紋 2～3 個 + 溫度／雨量 + 鄰近站狀態`

### 7.1 必做：消融實驗（ablation）

從最小集開始，一次加一批，記錄驗證 MAE：

```
lag + 時間                    → MAE ?
  + day_type                  → MAE ?
  + 站點識別 / 指紋            → MAE ?
  + 天氣                      → MAE ?
  + 鄰近站                    → MAE ?
  + POI / 坡度 / 事件          → MAE ?  ← 預期增益接近 0
```

**沒有消融表的特徵工程，等於沒有證據的宣稱。** 這張表同時是：
- 砍因子的 ADR 證據（滿足治理規範「不得無證據靜默決定」）
- 簡報回答「你怎麼知道這些特徵有用」的直接答案

九天內跑得完。

---

## 8. P2：低估或遺漏的事

### F-09｜沒有定義「成功」的營運指標 🟠 ← 這是最大的一個

`MAE 1.51` 是模型指標。交通局關心的是空站率、調度里程、民眾借不到車的次數。

模擬重放（尖峰空站率 7.19% → 5.45%）才是他們要的東西，但目前它被放在「原型成果」而非主要指標。

**建議把驗收指標明確切成三層**：

| 層 | 指標 | 誰在乎 | 現況 |
|----|------|--------|------|
| 模型層 | MAE、pinball loss、PICP／MPIW | 開發者 | 有 |
| **決策層** | 規則引擎觸發的 **precision / recall** | **這才是產品** | **完全沒有** |
| 營運層 | 空站率、派車數、平均反應時間 | 交通局 | 有（模擬） |

**決策層是完全缺失的一環，而它是模型與價值之間的橋。**

具體做法：對每一次規則引擎觸發，回頭檢查「接下來 30～60 分鐘內是否真的發生空站」→
- **precision** = 觸發的當中有多少是真的該觸發（衡量白跑率）
- **recall** = 真的發生的空站有多少被提前抓到（衡量漏報率）

模型 MAE 再好，`trigger.觸發靈敏度` 設錯，系統照樣沒用。**這三層對齊的那張圖，本身就是一頁很強的投影片。**

### F-10｜不確定性只做了一半 🟠

quantile regression 抓的是 **aleatoric**（資料固有隨機性），抓不到 **epistemic**（模型面對沒見過的情境）。

後果：颱風天模型會給出一個**很有信心的錯誤答案**，而區間不會變寬。

**修法**：加簡易分布外偵測——當前特徵值超出訓練分布範圍 → 標記低信心 → 退回規則層。

這剛好呼應 ADR-006 的降級策略，把它從「資料斷線時降級」擴充成「**模型沒把握時也降級**」。屬 ADR-006 的自然延伸，不需新 ADR。

### F-11｜服務公平性／區域均衡未納入 🟡

若模型對市中心大站準、對偏遠小站不準，調度資源會**系統性偏向市中心**。烏來、貢寮這類剛達標的站可能永遠排不到隊。

**而新北剛達成「區區有 YouBike」的政策宣示——區域均衡是他們自己講出來的政策目標。**

建議：
- 分行政區報模型表現（成本極低，就是 group by）
- 規則引擎加一條「服務公平性下限保障」

在政府黑客松，這是殺傷力很大的一段，且幾乎沒有隊伍會想到。

### 其他三項（低成本，順手做）

| 項目 | 說明 |
|------|------|
| **訓練 30 分鐘 vs 上線 1～5 分鐘的粒度落差** | 上線會拿到比訓練更細的資料，但模型沒學過。**Demo 建議聚合到 30 分鐘保持一致**，簡報講「未來可用更細粒度重訓」——把限制轉成 roadmap |
| **模擬重放的假設要誠實標示** | 7.19% → 5.45% 是模擬非實證，隱含「調度車準時到」「調度不影響需求」等假設。建議做敏感度分析（到達時間延遲 30% 時效果剩多少），評審會問 |
| **批次推論實作** | 1,583 站 × 4 horizon × 3 分位數 ≈ 19,000 個預測／每 30 分鐘。LightGBM 是毫秒級，但**別寫成逐站 for loop**，那會變成數十秒。一次批次 predict |

---

## 9. 確認正確、不需更動的設計

以下經審查確認方向正確，**可以安心往下走，不要在這些地方重新開會**：

| 設計 | ADR | 評價 |
|------|-----|------|
| **AI 只估計、規則引擎決策** | ADR-004 | **全專案最好的決策。** 政府系統需要可問責性——「為什麼派車」必須有人能回答。這個切分讓模型出錯時系統不會失控，也讓交通局能自己調整策略而不必碰模型 |
| 單一模型 + 分層參數 | ADR-001 | 正確。1,583 個模型是維運災難，且小站樣本不足、新站無解 |
| LightGBM + quantile | ADR-002 | 正確。表格資料 + 需要可解釋 + 需要區間，這是對的工具（實測數字待 P0 修正後更新） |
| 規則引擎吃區間下界 | ADR-004 | 正確，且與下游介面設計一致 |
| 時間切分、禁止隨機切分 | — | 必須的，做對了（但需擴充為 rolling CV，見 F-07） |
| 需求密度分規劃層／調度層 | ADR-104 | 這個分離很聰明。規劃層是給交通局的預算建議，調度層是即時決策，混用會出事 |
| 兩套分群對照（行為指紋 vs POI） | ADR-104 | 對，但它是**分析洞察，不是模型特徵**（見 F-08） |
| 因子的工程三維度分類 | ADR-103 | 對，而且這直接決定資料管線架構，是 PM 該做的事 |
| 學生數／外部人口因子降級為冷啟動 | ADR-104 | 對。**同樣邏輯請套用到坡度與 POI**（見 F-08） |

---

## 10. 動作清單（照優先序，含負責人建議）

| 序 | 動作 | 對應 | 負責 | 前置 |
|----|------|------|------|------|
| 1 | 修行為指紋／分群的計算窗口，改用訓練期或 expanding window | F-01 | A | — |
| 2 | 檢查缺失值填補是否用了 interpolate，改 forward fill | §5-1 | A | — |
| 3 | 實作 seasonal naive baseline，同驗證集比較 | F-02 | A | — |
| 4 | **重跑驗證，取得誠實的新數字；更正 ADR-002 的數字** | F-01～03 | A | 1,2,3 |
| 5 | 標記觸底／觸頂樣本，評估改為分區間 | F-03 | A/B | 4 |
| 6 | 改直接多視野 h=1～4；分位數直接對累積 Δ 訓練 | F-04/05 | B | proposed ADR |
| 7 | 補「該站同時段歷史 P50 淨流量」特徵 | F-06 | B | 1 |
| 8 | 換 rolling-origin CV，報平均 ± 標準差 | F-07 | A | 4 |
| 9 | 跑消融實驗，產出因子貢獻表 | F-08 | B | 7 |
| 10 | 補決策層指標（觸發 precision / recall） | F-09 | A | 5 |
| 11 | 分行政區表現報表 | F-11 | A | 8 |
| 12 | 有餘力：conformal 校準、分布外偵測、分位數成本掛鉤 | 6.5 / F-10 / 6.2 | B | 8 |

**動作 1～4 是一個不可分割的批次。** 做完之前，任何模型數字都不應寫進簡報或 ADR。

---

## 附錄 A：交給 Kiro 的起手 prompt

複製以下內容給 Kiro（或任何接手的 coding agent）：

```
請先讀 AGENTS.md、.kiro/steering/decision_governance.md、docs/decisions/README.md，
然後讀 review_model_pretraining_v1.md（進訓練前審查報告）。

這一輪只處理 P0：第 3 節的 F-01、F-02、F-03，以及第 5 節的洩漏檢查第 1、2、4 項。
第 4 節以後的內容這輪不要動，也不要先行實作。

依核准閘門，先給我計畫，不要改任何檔案。計畫要包含：

1. ADR 判斷：報告第 2 節已有建議判斷，請你自己覆核一次，不要照抄。
   逐項寫明「沿用 ADR-XXX / 不需要（原因）/ 需新增 proposed ADR」。
2. 現況查證：先用 git status 確認既有變更，並實際讀 code 確認
   （a）行為指紋與站型分群目前的計算窗口是什麼
   （b）缺失值填補用的是哪個方法
   （c）analysis_rebalancing_detection_v2.py 的判斷是否用到了「之後」的資料
   （d）停班停課標籤的來源是事前公告還是事後標註
   查證結果如果跟報告的推測不符，以你查到的為準，並告訴我哪裡不符。
3. 影響檔案清單與風險。
4. 驗證指令：修正前後要跑什麼、比較哪些數字。

特別注意：
- 本輪不得修改任何 accepted ADR 的決策內容，只能補記澄清（retrospective: true）。
- ADR-002 的實測數字（MAE 1.51 / baseline 4.29 / 覆蓋 78.7%）在重跑前不要動，
  重跑後才更正，且要保留舊數字與差異說明。
- 核心約束不可違反：AI 只估計、規則引擎決策，規則引擎吃區間下界。
```

## 附錄 B：後續批次

- **第二批（P1）**：F-04／F-05（多視野 + 分位數，需 proposed ADR，且影響 api_contract，要通知 B、C）、F-06、F-07
- **第三批（P2）**：F-08 消融實驗 → 憑結果開 ADR 砍因子；F-09 決策層指標；F-10／F-11

每批都在前一批「重跑並取得新數字」之後才開始。

---

*本報告為外部審查建議，非核准決議。所有涉及 accepted ADR 的變更，須依 `.kiro/steering/decision_governance.md` 流程由 owner 核准。*
