---
status: accepted
date: 2026-09-11
decision-makers:
  - project-owner
approval-evidence: '2026-09-11 本 Codex 對話：owner 於已提出三批修正範圍後明確表示「第二批也可以開始做了」。本檔先建立 proposed，再依此既有明確核准記錄 accepted；僅含第二批真實資料與服務整合。'
scope: [frontend, api]
related-commits: []
retrospective: false
supersedes: 
superseded-by:
---

# ADR-206：前端實際派工服務整合

## 背景與問題

第二批審查確認即時時間被改寫為歷史末尾、推論重新擬合特徵、詳情向即時 adapter 查歷史而失敗，以及前端仍在本機假確認／假完成。需要同時對齊資料與執行服務的契約。

## 決策

沿用 ADR-203、205 的視覺／角色頁面及 ADR-010 Demo 身分，落實 ADR-302。資料 adapter 以明確 api／mock 模式切換，預設連後端，同源 /api 由 Vite 開發代理；Mock 僅在明確選擇時使用，API 失敗不自動回 Mock。

受控 Demo 使用者需選擇後端帳號，寫入以 X-Operator-Id 傳送；預覽取得後端草稿與期限，確認只送 ID／version。司機以明確簽到／下班更新自己的值勤狀態（仍有任務不得下班，與派工同交易保護並記稽核）；司機讀已指派任務並以實際數量逐站回報，結案後重新讀取伺服器狀態。來源／過期／預測缺失可見；仍採 Mock 的孿生示範明確獨立標示。此身分機制不升格為正式認證。

## 理由與判準

保留真實來源與時間、失敗可辨識、沿用已核准框架，以可回歸驗證的服務行為完成第二批整合。

## 考慮過的替代方案

保留靜默 Mock fallback 容易把假數字當正式資料；每次推論重擬合既慢又與訓練不一致。新增雲端資料平台／正式認證非本次必要範圍，維持既有本機 Demo 限制。

## 影響與後果

資料缺失會顯示無預測／過期，部分原先看似可派工的站將被阻擋。多出特徵包及客戶端版本同步成本。正式認證、24 小時排程、多實例及第三批模型／路線修正仍待後續。

## 介面與相容性

保留 station_id 與原本主要欄位，新增來源及預測狀態；確認沿用 ADR-302 草稿契約。模式由設定選擇，歷史和 Mock 仍可明確使用。

## 資安與隱私

後端維持角色檢查，公開來源仍驗證 TLS／型別／大小。無新憑證、真實個資或第三方通知。不新增付費基礎設施。

## 回復或取代方式

可切回明確 Mock 模式或停用寫入；特徵包須與模型成套回復，不能重新開放靜默假資料。後續改變依新 ADR supersede。

## 驗證方式

pytest 以固定時鐘與離線來源驗證時間／過期／斷線、模型特徵及派工重驗；API 流程測試；前端 transport 測試及 npm run build；容器設定與依賴檢查；git diff --check。實際執行結果另記工作報告。

## 追溯

- 核准範圍：本對話第二批「真實資料與服務整合」。
- 相關 Spec：api_contract、model_architecture、design。
- 相關 commit：尚無；本次不 commit／push。
