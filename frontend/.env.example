# api connects to FastAPI through Vite; mock explicitly enables the isolated demo.
VITE_DATA_MODE=api
# Production reverse proxy should expose /api/v1 on the frontend origin.
VITE_API_BASE_URL=/api/v1
# Vite dev server proxy (server-side environment only)
YOUBIKE_BACKEND_URL=http://127.0.0.1:8000
